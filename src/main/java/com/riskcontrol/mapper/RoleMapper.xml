<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.riskcontrol.dao.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.riskcontrol.domain.Role">
        <result column="id" property="id" />
        <result column="deleted" property="deleted" />
        <result column="create_id" property="createId" />
        <result column="create_time" property="createTime" />
        <result column="modified_id" property="modifiedId" />
        <result column="modified_time" property="modifiedTime" />
        <result column="name" property="name" />
        <result column="is_default_role" property="isDefaultRole" />
    </resultMap>

    <select id="queryPage" resultType="com.wuyuan.robot.domain.vo.role.RolePage">
        SELECT
            r.id,
            r.name,
            r.is_default_role,
            r.create_time,
            r.modified_time updateTime,
            u.name createUserName
        FROM role r
        LEFT JOIN user u on r.create_id = u.id
        WHERE r.deleted = 0
          <if test="query.name != null and query.name != ''">
              AND r.name like concat('%',#{query.name},'%')
          </if>
    </select>
</mapper>
